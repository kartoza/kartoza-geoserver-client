# Architecture

CloudBench is a full-stack application with Python backend and React frontend.

## Overview

```
┌─────────────────────────────────────────────────────────┐
│                      Web Browser                         │
│  ┌─────────────────────────────────────────────────────┐│
│  │              React + Chakra UI Frontend              ││
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ ││
│  │  │  MapLibre   │  │   Zustand   │  │   React     │ ││
│  │  │   GL JS     │  │   Stores    │  │   Query     │ ││
│  │  └─────────────┘  └─────────────┘  └─────────────┘ ││
│  └─────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘
                            │
                    REST API (JSON)
                            │
┌─────────────────────────────────────────────────────────┐
│                   Django Backend                         │
│  ┌─────────────────────────────────────────────────────┐│
│  │           Django REST Framework                      ││
│  └─────────────────────────────────────────────────────┘│
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐ │
│  │  GeoServer  │  │  PostgreSQL │  │   S3 Storage    │ │
│  │   Client    │  │   Client    │  │    Client       │ │
│  └─────────────┘  └─────────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────┘
                            │
              ┌─────────────┼─────────────┐
              │             │             │
        ┌─────────┐   ┌─────────┐   ┌─────────┐
        │GeoServer│   │PostgreSQL│  │  MinIO  │
        │  REST   │   │ PostGIS │   │   S3    │
        └─────────┘   └─────────┘   └─────────┘
```

## Backend Structure

```
apps/
├── accounts/       # User authentication
├── connections/    # Connection management
├── core/           # Shared utilities
├── geoserver/      # GeoServer REST client
├── gwc/            # GeoWebCache management
├── postgres/       # PostgreSQL integration
├── preview/        # Layer preview sessions
├── s3/             # S3 storage client
├── upload/         # Chunked file uploads
└── ...
```

## Frontend Structure

```
web/src/
├── api/            # API client modules
│   ├── client.ts   # Main API functions
│   ├── connection.ts
│   ├── layer.ts
│   └── ...
├── components/     # React components
│   ├── MapPreview.tsx
│   ├── MainContent.tsx
│   └── ...
├── stores/         # Zustand state stores
│   ├── treeStore.ts
│   ├── uiStore.ts
│   └── ...
└── types/          # TypeScript types
```

## Key Technologies

### Backend
- **Django 5.1**: Web framework
- **Django REST Framework**: API toolkit
- **httpx**: Async HTTP client
- **boto3**: AWS S3 client

### Frontend
- **React 18**: UI library
- **Chakra UI**: Component library
- **Zustand**: State management
- **React Query**: Data fetching
- **MapLibre GL JS**: Map rendering

### TUI
- **Textual**: Python TUI framework
